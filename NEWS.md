# scMetaTraj 0.1.2

- restored package-level documentation for GitHub and CRAN, including a
  generated README and a runnable workflow vignette
- exported the core workflow helpers for scoring, embedding, cluster profiling,
  and metabolic pseudotime inference
- clarified package metadata, repository URLs, and CRAN submission notes
- tightened build exclusions so repository-only files are not bundled into the
  source tarball

# scMetaTraj 0.1.1

- initial public package release

